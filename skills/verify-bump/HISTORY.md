# Skill History

2026-07-30 · claude-config · 1166 words · created
2026-08-02 · tcg-card-collector · 1166 words · retro minor: DIRTY rule said stop where landing needed self-integration; merge corrected to rebase mid-run; Gather pulled the 15k-char PR body and unfiltered --log-failed every run; parked mechanisms: mutually-blocking sibling PRs invisible to single-PR scope, and Gather-as-script if the new grep flags regress
2026-08-02 · tcg-card-collector · 1406 words · fix big: DIRTY now means integrate the base yourself, rebase replaces merge and gains a force-with-lease push, merge method reads repo history, and Gather greps the PR body and failed-log instead of pulling them whole
2026-08-03 · claude-config · 1151 words · compacted: merged the duplicated CI-gap and same-session-report rules into one home each, cut the base-tip and PIPESTATUS rationales after fixing the merge-base rule they contradicted, dropped the one-run compiled-output case, and trimmed the description to triggers
2026-08-03 · claude-config · 1151 words · retro minor: gh --log-failed grep had no leading context so the tool diagnostic sat above the matched runner line unread, needing a second call; parked mechanisms: fresh worktree lacks gitignored build inputs (install+codegen) so absent generated modules read as bump breakage, and the failure branch stops at the push without covering wait-for-green-then-land
2026-08-03 · claude-config · 1233 words · fix big: failure branch now proposes an investigation and waits for a go-ahead before building a fix, replacing the implement-on-confirmation step; --log-failed grep gained -B20 so the diagnostic above the runner exit line is captured
2026-09-13 · tcg-card-collector · 1233 words · retro minor: merge-style check listed old merge commits while recent bumps were squashes, needing a parent-count recheck (fix deferred by user); parked mechanisms: no changelog anywhere so npm tarball d.ts diff stood in, and stale green CI on a >30-day PR landed on local rebase verification without re-run; filed repo#88 for unprovisioned Playwright browser
2026-09-13 · claude-config · 1233 words · retro minor: fresh worktrees lacked gitignored codegen (second sighting, redirected to a general worktree-provisioning home rather than this skill) and the failure branch left push-to-land unstated (second sighting, fixed); push lease SHA was never gathered; merge-style check worked around again, user prefers a stated default over reading history (pending); parked mechanism: auto-mode classifier blocks the force-push, fallback is a user-run ! command
2026-09-13 · claude-config · 1271 words · fix big: failure branch's push confirmation now also covers landing: merge method asked with it, then watch checks and merge pinned to the pushed SHA; Gather query fetches headRefOid for the push lease
2026-09-13 · claude-config · 1248 words · fix big: clean-pass merge method now comes from a stated CLAUDE.md landing default instead of reading past merges from history; worktree provisioning moved to CLAUDE.md as a cross-skill rule
2026-09-16 · claude-config · 1248 words · retro minor: clean pass after self-integrating a DIRTY PR had no push path (push steps lived only in the failure branch), and a lease rejection from a mid-run bot rebase matched no listed rejection cause; parked mechanisms: bot auto-rebase still enabled races local integration, and lockfile regeneration from base floats in-range versions past the bump's pins
2026-09-16 · claude-config · 1302 words · fix big: push steps moved into a shared Push & land step that a clean pass on a rebased branch now reaches too, and a lease rejection now means re-verify and land the author's moved head instead of a superseding branch
