# Skill History

2026-07-30 · claude-config · 698 words · created
2026-08-02 · tcg-card-collector · 768 words · retro major: Find candidates carries no composition test, so the pass proposed 106 words and found 174 more only after the user asked about motivation text; never project the final ratio did not bind on a word-count projection; the first mandated command failed because dotnet run from the session repo deletes the previous build runtimeconfig; deferred with no fix: Finish records the baseline without saying it must follow the last edit, so 1218 was logged and had to be reverted
2026-08-02 · tcg-card-collector · 828 words · fix big: Find candidates gained the composition test author-skill applies at drafting, the projection ban now covers word counts and baselines, and bin/skill-refiner.sh replaces dotnet run at all five call sites so a build in the caller cwd cannot delete the runtime files
2026-08-02 · claude-config · 828 words · retro minor: candidate sizes quoted before the pick summed 62% above the file actual fall, because the drafted-replacement measurement the rule prescribes is unaffordable at presentation time and got replaced with an eyeballed cut-whole-vs-residue split that was itself never measured; deferred with no fix: just shell lints only tracked files, so a new script under scripts/ passes just check vacuously until it is staged
2026-08-02 · claude-config · 800 words · fix small: the savings rule quotes a measured block instead of a drafted replacement, and its two war stories moved to the commit message
2026-08-03 · claude-config · 675 words · compacted: cut the justifications behind six rules — the motivation bullet own story, the don-t-decide-alone epistemics, the ladder one-home sentence, two Scope rationales, git blame, and two tails — leaving the instructions
2026-08-03 · claude-config · 675 words · retro minor: git log -p cost 46KB where the commit messages cost 6KB and carried every candidate’s evidence; "the measured size of its block" left the extent unsaid and was quoted as a clause and as its enclosing bullet in one list; deferred with no fix: Scope’s counts-against list rules on neither contradictions the pass finds nor cuts of the same class as a picked candidate, and Find candidates covers the body only, so the description’s word bar in skill-conventions.md is reachable only by accident
2026-08-03 · claude-config · 710 words · fix small: the history read names the commit-message format and pulls patches per-commit, and candidate sizes are quoted as the clause the candidate would touch rather than an unspecified block
